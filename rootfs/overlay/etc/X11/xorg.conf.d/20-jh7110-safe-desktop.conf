# Keep the login desktop independent of the experimental PVR/Mesa GL pairing.
# Vulkan/EGL applications may still test the installed GPU runtime separately.
Section "Device"
    Identifier "JH7110 display controller"
    Driver "modesetting"
    Option "AccelMethod" "none"
EndSection
